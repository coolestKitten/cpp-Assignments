#include <vector>
#include <iostream>

using namespace std;



void constructMatrix(int nodeAmt, vector<int> values){
    // first matrix, contains paths
    vector<vector<int> > paths;

    // second matrix, the one we are modifying
    vector<vector<int> > values;

    for(int i = j)


}

int main(){
    return 0;
}